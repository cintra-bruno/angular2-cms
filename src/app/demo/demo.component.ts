import { Component, OnInit } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'demo',
  template: `
    Empty page!
  `
})
export class DemoComponent implements OnInit {

  ngOnInit(): void {
    // stub
  }
}
